import React from "react"
import MainLayout from "./MainLayout";

const UserInfo = () => {
    return (
        <MainLayout>
            <h1 style={{color: "black" }}>
                This is a UserInfo Page
            </h1>
        </MainLayout>
    );   
};

export default UserInfo;