import React from "react"
import MainLayout from "./MainLayout";

const LogTransaction = () => {
    return (
        <MainLayout>
            <h1 style={{color: "black" }}>
                This is a LogTransaction Page
            </h1>
        </MainLayout>
    );   
};

export default LogTransaction;