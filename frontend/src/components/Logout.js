import React from "react"
import MainLayout from "./MainLayout";

const Logout = () => {
    return (
        <MainLayout>
            <h1 style={{color: "black" }}>
                This is a Log Out Page
            </h1>
        </MainLayout>
    );   
};

export default Logout;